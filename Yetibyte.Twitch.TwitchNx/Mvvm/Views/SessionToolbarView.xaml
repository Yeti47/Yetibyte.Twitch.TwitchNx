<UserControl x:Class="Yetibyte.Twitch.TwitchNx.Mvvm.Views.SessionToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:local="clr-namespace:Yetibyte.Twitch.TwitchNx.Mvvm.Views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Horizontal" Height="32">
            <Button Style="{DynamicResource MahApps.Styles.Button.ToolBar}"
                    Command="{Binding StartSessionCommand}">
                <Image Source="/Images/play.png" Width="20" Height="20" />
            </Button>
            <Button Style="{DynamicResource MahApps.Styles.Button.ToolBar}"
                    Command="{Binding StopSessionCommand}">
                <Image Source="/Images/stop.png" Width="20" Height="20" />
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
