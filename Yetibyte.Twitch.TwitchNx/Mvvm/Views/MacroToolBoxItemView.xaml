<UserControl x:Class="Yetibyte.Twitch.TwitchNx.Mvvm.Views.MacroToolBoxItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Yetibyte.Twitch.TwitchNx.Mvvm.Views"
             xmlns:db="clr-namespace:Yetibyte.Twitch.TwitchNx.DataBinding"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Background="{DynamicResource MahApps.Brushes.Accent}"
                Width="120"
                Height="120"
                CornerRadius="8"
                >
            <Border.Effect>
                <DropShadowEffect />
            </Border.Effect>

            <local:MacroInstructionTemplateView DataContext="{Binding MacroInstructionTemplateViewModel}" />

        </Border>
    </Grid>
</UserControl>
